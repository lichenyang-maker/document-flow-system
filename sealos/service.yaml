apiVersion: v1
kind: Service
metadata:
  name: document-flow-system
  namespace: document-flow
  labels:
    app: document-flow-system
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 3000
      protocol: TCP
      name: http
  selector:
    app: document-flow-system
